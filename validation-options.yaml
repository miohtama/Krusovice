linelength:
  length: 250

# Python additional configuration

jshint:

  configuration: |
            {
                "globalstrict" : true
            }

# No need to wind up pylint for our puny files
pylint:
    enabled: false